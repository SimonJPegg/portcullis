version: "2"

linters:
  enable:
    - errcheck
    - exhaustive # the system constrains correctness
    - govet
    - staticcheck
    - gocyclo
    - ineffassign
    - unused
  settings:
    gocyclo:
      min-complexity: 10

run:
  timeout: 5m
